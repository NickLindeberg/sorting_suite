pry = require('pryjs')

function flip(array){
  sorted_array = array.reverse();
  return sorted_array
};

function findHigh(ary){
  high_num = Math.max(...ary);
  return high_num
};


function sort(array) {
  length = array.length

}

console.log(sort([1,2]))
